import { Outlet } from "react-router-dom";

const Services = () => {
    return (
        <div>
            <main>
                <Outlet />
            </main>
        </div>
    );
};

export default Services;
